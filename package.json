{
  "name": "axon-webflow-bundle",
  "version": "1.0.0",
  "description": "CDN bundle for Webflow custom interactions",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "preview": "vite preview",
    "version:patch": "npm version patch",
    "version:minor": "npm version minor",
    "version:major": "npm version major",
    "release": "npm run build && git add dist/main.iife.js && git commit -m \"Build dist\" && git push && git push --tags"
  },
  "devDependencies": {
    "terser": "^5.31.0",
    "vite": "^5.4.0"
  }
}

